type User @entity {
  " accountAddress "
  id: ID!
}

type Reward @entity {
  " tokenAddress-tokenId "
  id: ID!
  tokenAddress: String!
  tokenId: BigInt!
  balances: [AccountBalance!]! @derivedFrom(field: "reward")
}

type AccountBalance @entity {
  " rewardId-userId "
  id: ID!
  user: User!
  reward: Reward!
  amountOwned: BigInt!
  amountMinted: BigInt!
}
